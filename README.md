# CryptoTrack
A dark, data-dense portfolio dashboard for tracking crypto holdings. Built as a frontend-only project with HTML5, SCSS (BEM), and vanilla JavaScript ES6 modules — no frameworks, no bundler, no backend. The only runtime dependencies are Chart.js and two Google Fonts.
